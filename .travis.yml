language: python

python:
  - 2.7
  - 3.5
  - 3.6
# GPyOpt doesn't yet work for 3.7

install:
  - pip install -U pip setuptools setuptools_scm
  - pip install -r requirements.txt

script:
  - python setup.py test
